const router = require('express').Router()


// Get All -GET-
router.get('/', (req, res) => {
    res.send('GET /places')
})

// Get One -GET-


// Creating One -POST-


// Update One -PUT-


// Deleteing One -DELETE-


module.exports = router

// use req.params.id to get access to whats passed in after the first /